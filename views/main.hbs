<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MongoDBPractice</title>
</head>
<body>
    <h1>
        MongoDBPractice
    </h1>
    {{message}}
    <br>

    {{#if edit_id}}
    <h3>Edit:</h3>
    <form method="post" action="/update_book/{{edit_id}}">
        <label for="">Title</label>
        <input type="text" name="Title" value="{{edit_book.title}}">
        <label for="">Author</label>
        <input type="text" name="Author" value="{{edit_book.author}}">
        <input type="submit" name="" id="">
    </form><br><br>

    {{else}}
    <h3>Create:</h3>
    <form method="post" action="/store_book">
        <label for="">Title</label>
        <input type="text" name="Title">
        <label for="">Author</label>
        <input type="text" name="Author">
        <input type="submit" name="" id="">
    </form><br><br>
    {{/if}}
    <ul>
        {{#each books}}
        <li>{{this.title}}-{{this.author}} <a href="/?edit_id={{this._id}}">Edit</a> <a href="/?delete_id={{this._id}}" onclick="return confirm('Are you sure to delete this?')">Delete</a> </li>
        {{/each}}
    </ul>
</body>
</html>